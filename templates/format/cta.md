# 文末获客 CTA 组件（企微承接 + 双诱饵）

> 用途：注入到每篇公众号文章正文末尾，实现「发文 → 企微承接 → 诱饵发放 → 转化跟进」的获客闭环。
> 更新：2026-09-23，杨辉老师定稿（承接统一走企业微信；诱饵为「资料包 + 免费试用工具」双钩子）。

---

## 一、CTA 组件（嵌入正文末尾的最终形态）

```markdown
---

## 🎁 想系统掌握 AI 量化？这里有一份见面礼

看完还不过瘾？我把更完整的实战框架整理好了，加我微信直接发你。

**👇 长按识别二维码，3 秒添加，资料立即送达**

![企业微信二维码]({{WECOM_QR_IMAGE}})

添加后回复对应数字，免费领取：

① 回复 **【1】** → 领取《AI量化入门资料包》（选股 · 风控 · 仓位管理核心框架）
② 回复 **【2】** → 申请万丰 AI 量化工具 · 免费试用名额
③ 回复 **【3】** → 预约一对一诊断

> 本内容仅供学习交流，不构成投资建议。
```

---

## 二、占位符说明

| 占位符 | 说明 | 填充方式 |
|--------|------|----------|
| `{{WECOM_QR_IMAGE}}` | 企业微信获客**客户群活码**图片 | 固定取 `config/wecom-qr.png`（活码长期有效，多群轮换自动换群）；缺失时以文字引导「微信搜索：AI量化杨教练」兜底 |

---

## 附：更换活码（换码即全链自动复用）

```bash
bash scripts/rotate_wecom_qr.sh <新活码图片路径> [--expires never|YYYY-MM-DD] [--dry-run]
```

- 新活码获取：企业微信后台 →「客户联系 → 加入群聊 → 客户群活码」，一般**长期有效**（`--expires never`）。
- 脚本做三件事：备份旧码 → 覆盖 `config/wecom-qr.png` → 更新 meta 与台账。
- **format 只认固定路径**，换码后无需改文章、模板或脚本。

---

## 三、注入规则（format 环节强制）

1. **注入位置**：正文「风险提示」之后、文章最末尾。
2. **注入条件**：QA 质检 ≥80 分且已过审的文章，全部注入（获客是每篇的固定收尾）。
3. **二维码处理（活码自动复用）**：
   - format **只认固定路径** `config/wecom-qr.png`：只要该文件存在，就作为配图插入，并随其他配图一起走 `baoyu-compress-image` 压缩。
   - 因此**换码无需改任何文章/模板**：把新活码覆盖 `config/wecom-qr.png` 即可，format 下次跑自动用新码（建议跑 `scripts/rotate_wecom_qr.sh` 同步 meta 与台账）。
   - 若缺失 → 用文字引导「微信搜索：AI量化杨教练」替代，**不阻断发文**，但需在 `04_publish_queue.md` 备注栏标记「待补二维码」。
4. **有效期感知**：format 启动时读 `config/wecom-qr.meta.json`；若 `never_expires=true` 直接复用；若已过期或 3 天内到期，则在 `04_publish_queue.md` 备注栏标记「二维码待更换」，并提醒主控更新。
5. **一句话校验**：注入后正文末尾必须含「添加后回复对应数字」+ 三个诱饵，否则视为注入失败，打回 format 重做。

---

## 四、钩子话术库（可复用，随 RSI 台账迭代）

| 场景 | 话术 |
|------|------|
| 通用见面礼 | 「👇 长按识别二维码，3 秒添加，资料立即送达」 |
| 资料包诱饵 | 「① 回复【1】领取《AI量化入门资料包》」 |
| 试用诱饵 | 「② 回复【2】申请量化工具免费试用」 |
| 诊断诱饵 | 「③ 回复【3】预约一对一诊断」 |
| 兜底文字 | 「微信搜索：AI量化杨教练」 |

> 话术随人工修正记录进 `_rsi_ledger.md`「金句库」，不断迭代。
